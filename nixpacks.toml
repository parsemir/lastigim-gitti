[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = [
  "cd client && npm run build",
  "cd server && npx prisma generate && npx prisma migrate deploy && npm run build"
]

[start]
cmd = "cd server && NODE_ENV=production node dist/index.js"
